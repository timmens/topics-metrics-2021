name: topics-metrics

channels:
  - defaults
  - conda-forge
  - r

dependencies:
  - python=3.9

  - ipykernel
  - jupyterlab
  - nb_conda_kernels
  - rise

  - pandas
  - numpy
  - scikit-learn
  - matplotlib
  - seaborn

  - pytask
  - pytask-r

  - pip
  - pip:
    - manimgl

  - rpy2
  - r-base
  - r-essentials
  - r-devtools
  - r-rjava

  - black
  - flake8
  - pre-commit

# $ R
# $ Sys.setenv(TAR = "/bin/tar")
# $ devtools::install_github("user/repo", ref="main", dependencies=FALSE)
